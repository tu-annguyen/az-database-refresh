{
  "name": "@az-refresh/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "XDG_CONFIG_HOME=.wrangler/config wrangler dev src/index.ts --local --persist-to=.wrangler/state",
    "build": "XDG_CONFIG_HOME=.wrangler/config wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@az-refresh/shared": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.107.0"
  }
}
